# 📁 config/ - Django Configuration

This directory contains Django configuration files.

## Structure

- **settings.py** - Main Django settings
- **urls.py** - Root URL configuration
- **wsgi.py** - WSGI application configuration
- **asgi.py** - ASGI application configuration

## Usage

These files configure Django's core functionality including apps, middleware, database, and static files.
